To install magento2 run the following command in the terminal:
docker cp install_magento2.sh dockermagento_magento_1:/home/ && docker exec dockermagento_magento_1 chmod +x /home/install_magento2.sh && docker exec dockermagento_magento_1 /bin/sh /home/install_magento2.sh testmagento.org

This command copies installation script to container and runs it. Configure site name as the last argument of the above command.
